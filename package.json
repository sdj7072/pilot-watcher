{
  "name": "pilot-watcher-root",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx}": [
      "npm run lint --prefix frontend",
      "npm run test --prefix frontend -- related --run"
    ]
  }
}